name: macOS

on: [push]

jobs:
  macOS:
    name: macOS
    runs-on: macOS-latest

    steps:
      - name: Setup
        run: /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/myako10s/dotfiles/HEAD/install.sh)"
